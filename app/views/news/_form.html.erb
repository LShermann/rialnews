<%= form_with(model: new) do |form| %>
  <% if new.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(new.errors.count, "error") %> prohibited this new from being saved:</h2>

      <ul>
        <% new.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :tittle, style: "display: block" %>
    <%= form.text_field :tittle %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
